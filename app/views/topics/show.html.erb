 <h1><%= @topic.title %></h1>
    <div class="col-md-2">
        <%= link_to "Edit", edit_topic_path, class: 'btn btn-success' %> or
        <%= link_to "Delete", @topic, method: :delete, class: 'btn btn-danger', data: { confirm: 'This is my favorite Topic, are you sure?' } %> 
         this topic
    </div>
<br>
<div class="row">
    <div class='col-md-8'>
         <% @topic.bookmarks.each do |bookmark| %>
         <li><%= link_to bookmark.url, topic_bookmark_path(@topic, bookmark) %> </li>
            <div class="button_placement">
                <%= render partial: 'likes/like', locals: { bookmark: bookmark } %>
            </div>
         <% end %>
    </div>
</div>
 <br>
    <div class='col-md-2'>
        <%= link_to "Add a new bookmark", new_topic_bookmark_path(@topic), class: 'btn btn-success' %>
        <!--%= link_to "Edit", edit_topic_bookmark_path(@bookmark, @topic), class: 'btn btn-success' %> |-->
        <!--%= link_to "Delete", (@topic, @bookmark), method: :delete, class: 'btn btn-danger', data: { confirm: 'Noooo, this bookmark? really?' } %>-->
    </div>
    
 </div>